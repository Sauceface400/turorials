def raise_to_the_power(number, power):
    result = 1
    for index in range(power):
        result = result * number
    return result
print(raise_to_the_power(3,5))
#============================EXAMPLE'S================================================